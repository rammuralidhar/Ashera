//
//  Generated by the J2ObjC translator.  DO NOT EDIT!
//  source: /Users/ramm/git/Ashera/AsheraAndroidParser/freemarker/freemarker/core/CustomAttribute.java
//

#ifndef _FreemarkerCoreCustomAttribute_H_
#define _FreemarkerCoreCustomAttribute_H_

#include "J2ObjC_header.h"

@class FreemarkerCoreEnvironment;
@class FreemarkerTemplateConfiguration;
@class FreemarkerTemplateTemplate;

#define FreemarkerCoreCustomAttribute_SCOPE_ENVIRONMENT 0
#define FreemarkerCoreCustomAttribute_SCOPE_TEMPLATE 1
#define FreemarkerCoreCustomAttribute_SCOPE_CONFIGURATION 2

/**
 @brief A class that allows one to associate custom data with a Configuration , a Template , or Environment .
 <p>This API has similar approach to that of ThreadLocal (which allows one to associate custom data with a thread). With an example:</p> <pre> // The object identity itself will serve as the attribute identifier; there's no attribute name String: public static final CustomAttribute MY_ATTR = new CustomAttribute(CustomAttribute.SCOPE_CONFIGURATION); ... // Set the attribute in this particular Configuration object: MY_ATTR.set(myAttrValue, cfg); ... // Read the attribute from this particular Configuration object: myAttrValue = MY_ATTR.get(cfg); </pre>
 */
@interface FreemarkerCoreCustomAttribute : NSObject

#pragma mark Public

/**
 @brief Creates a new custom attribute with the specified scope
 @param scope one of <tt>SCOPE_</tt> constants.
 */
- (instancetype)initWithInt:(jint)scope;

/**
 @brief Same as #get(Environment) , but uses Environment#getCurrentEnvironment() to fill the 2nd argument.
 @throws IllegalStateException If there is no current Environment , which is usually the case when the current thread isn't processing a template.
 */
- (id)get;

/**
 @brief Gets the value of a Configuration -scope attribute from the given Configuration .
 @throws UnsupportedOperationException If this custom attribute has different scope than #SCOPE_CONFIGURATION .
 @throws NullPointerException If <code>cfg</code> is null
 @since 2.3.22
 */
- (id)getWithFreemarkerTemplateConfiguration:(FreemarkerTemplateConfiguration *)cfg;

/**
 @brief Gets the attribute from the appropriate scope that's accessible through the specified Environment .
 If the attribute has #SCOPE_ENVIRONMENT scope, it will be get from the given Environment directly. If the attribute has #SCOPE_TEMPLATE scope, it will be get from the parent of the given Environment (that is, in Environment#getParent() ) directly). If the attribute has #SCOPE_CONFIGURATION scope, it will be get from Environment#getConfiguration() .
 @throws NullPointerException If <code>env</code> is null
 @return The new value of the attribute (possibly <code>null</code> ), or <code>null</code> if the attribute doesn't exist.
 @since 2.3.22
 */
- (id)getWithFreemarkerCoreEnvironment:(FreemarkerCoreEnvironment *)env;

/**
 @brief Gets the value of a Template -scope attribute from the given Template .
 @throws UnsupportedOperationException If this custom attribute has different scope than #SCOPE_TEMPLATE .
 @throws NullPointerException If <code>template</code> is null
 */
- (id)getWithFreemarkerTemplateTemplate:(FreemarkerTemplateTemplate *)template_;

/**
 @brief Same as #set(Object,Environment) , but uses Environment#getCurrentEnvironment() to fill the 2nd argument.
 @throws IllegalStateException If there is no current Environment , which is usually the case when the current thread isn't processing a template.
 */
- (void)setWithId:(id)value;

/**
 @brief Sets the value of a Configuration -scope attribute in the given Configuration .
 @param value The new value of the attribute. Can be <code>null</code> .
 @throws UnsupportedOperationException If this custom attribute has different scope than #SCOPE_CONFIGURATION .
 @throws NullPointerException If <code>cfg</code> is null
 @since 2.3.22
 */
- (void)setWithId:(id)value
withFreemarkerTemplateConfiguration:(FreemarkerTemplateConfiguration *)cfg;

/**
 @brief Sets the attribute inside the appropriate scope that's accessible through the specified Environment .
 If the attribute has #SCOPE_ENVIRONMENT scope, it will be set in the given Environment directly. If the attribute has #SCOPE_TEMPLATE scope, it will be set in the parent of the given Environment (that is, in Environment#getParent() ) directly). If the attribute has #SCOPE_CONFIGURATION scope, it will be set in Environment#getConfiguration() .
 @param value The new value of the attribute. Can be <code>null</code> .
 @throws NullPointerException If <code>env</code> is null
 @since 2.3.22
 */
- (void)setWithId:(id)value
withFreemarkerCoreEnvironment:(FreemarkerCoreEnvironment *)env;

/**
 @brief Sets the value of a Template -scope attribute in the given Template .
 @param value The new value of the attribute. Can be <code>null</code> .
 @throws UnsupportedOperationException If this custom attribute has different scope than #SCOPE_TEMPLATE .
 @throws NullPointerException If <code>template</code> is null
 */
- (void)setWithId:(id)value
withFreemarkerTemplateTemplate:(FreemarkerTemplateTemplate *)template_;

#pragma mark Protected

/**
 @brief This method is invoked when #get() is invoked without #set(Object) being invoked before it to define the value in the current scope.
 Override it to create the attribute value on-demand.
 @return the initial value for the custom attribute. By default returns null.
 */
- (id)create;

@end

J2OBJC_EMPTY_STATIC_INIT(FreemarkerCoreCustomAttribute)

J2OBJC_STATIC_FIELD_GETTER(FreemarkerCoreCustomAttribute, SCOPE_ENVIRONMENT, jint)

J2OBJC_STATIC_FIELD_GETTER(FreemarkerCoreCustomAttribute, SCOPE_TEMPLATE, jint)

J2OBJC_STATIC_FIELD_GETTER(FreemarkerCoreCustomAttribute, SCOPE_CONFIGURATION, jint)

FOUNDATION_EXPORT void FreemarkerCoreCustomAttribute_initWithInt_(FreemarkerCoreCustomAttribute *self, jint scope);

FOUNDATION_EXPORT FreemarkerCoreCustomAttribute *new_FreemarkerCoreCustomAttribute_initWithInt_(jint scope) NS_RETURNS_RETAINED;

J2OBJC_TYPE_LITERAL_HEADER(FreemarkerCoreCustomAttribute)

#endif // _FreemarkerCoreCustomAttribute_H_
