//
//  Generated by the J2ObjC translator.  DO NOT EDIT!
//  source: /Users/ramm/git/Ashera/AsheraAndroidParser/freemarker/freemarker/template/TemplateDirectiveModel.java
//

#ifndef _FreemarkerTemplateTemplateDirectiveModel_H_
#define _FreemarkerTemplateTemplateDirectiveModel_H_

#include "J2ObjC_header.h"
#include "TemplateModel.h"

@class FreemarkerCoreEnvironment;
@class IOSObjectArray;
@protocol FreemarkerTemplateTemplateDirectiveBody;
@protocol JavaUtilMap;

/**
 @brief "directive" template language data type: used as user-defined directives (much like macros) in templates.
 They can do arbitrary actions, write arbitrary text to the template output, and trigger rendering of their nested content for any number of times. <p>They are used in templates like <code><@@myDirective foo=1 bar="wombat">...</@@myDirective></code> (or as <code><@@myDirective foo=1 bar="wombat" /></code> - the nested content is optional).
 @since 2.3.11
 */
@protocol FreemarkerTemplateTemplateDirectiveModel < FreemarkerTemplateTemplateModel, NSObject, JavaObject >

/**
 @brief Executes this user-defined directive; called by FreeMarker when the user-defined directive is called in the template.
 @param env the current processing environment. Note that you can access the output java.io.Writer Writer by Environment#getOut() .
 @param params the parameters (if any) passed to the directive as a map of key/value pairs where the keys are String -s and the values are TemplateModel instances. This is never <code>null</code>. If you need to convert the template models to POJOs, you can use the utility methods in the DeepUnwrap class.
 @param loopVars an array that corresponds to the "loop variables", in the order as they appear in the directive call. ("Loop variables" are out-parameters that are available to the nested body of the directive; see in the Manual.) You set the loop variables by writing this array. The length of the array gives the number of loop-variables that the caller has specified. Never <code>null</code>, but can be a zero-length array.
 @param body an object that can be used to render the nested content (body) of the directive call. If the directive call has no nested content (i.e., it's like &lt;@@myDirective /&gt; or &lt;@@myDirective&gt;&lt;/@@myDirective&gt;), then this will be <code>null</code>.
 @throws TemplateException If any problem occurs that's not an IOException during writing the template output.
 @throws IOException When writing the template output fails.
 */
- (void)executeWithFreemarkerCoreEnvironment:(FreemarkerCoreEnvironment *)env
                             withJavaUtilMap:(id<JavaUtilMap>)params
    withFreemarkerTemplateTemplateModelArray:(IOSObjectArray *)loopVars
 withFreemarkerTemplateTemplateDirectiveBody:(id<FreemarkerTemplateTemplateDirectiveBody>)body;

@end

J2OBJC_EMPTY_STATIC_INIT(FreemarkerTemplateTemplateDirectiveModel)

J2OBJC_TYPE_LITERAL_HEADER(FreemarkerTemplateTemplateDirectiveModel)

#endif // _FreemarkerTemplateTemplateDirectiveModel_H_
